

def main(params):

    assert params.path.lower().endswith(".dcm"), "Input file has wrong extension (should be dicom (.dcm or .DCM))"

    print(params.path)
    print(params.path)
    print(params.path)
